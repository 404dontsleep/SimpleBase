import ScrollView from "../components/layouts/ScrollView";

export default function Home() {
  return (
    <ScrollView
      style={{
        flex: 1,
        gap: 10,
        padding: 10,
      }}
    ></ScrollView>
  );
}
